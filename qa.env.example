# Copy this file to qa.env (gitignored) and fill in the real value.
# This is the password for the QA-only accounts qa-admin / qa-coach /
# qa-parent @example.com. The QA admin has the admin role on production,
# so never commit the real value.
QA_PASSWORD=replace_me
